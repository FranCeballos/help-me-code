import classes from "./SlidersContainer.module.css";

const SlidersContainer = () => {
  return <div className={classes["sliders__container"]}>SlidersContainer</div>;
};

export default SlidersContainer;
